// Given a string, return an object with all the unique letters of the string as keys, and the number of times this letter is used in the string as values.

 let statement = 'The Flinstones Rock';
// { T: 1, h: 1, e: 2, F: 1, l: 1, ... }

let arr = [1, 2, 3, 4];
let squaredNumbers = [];

for (let i = 0; i < arr.length; i++) {
  squaredNumbers.push(arr[i] * arr[i])
}

console.log(squaredNumbers)

